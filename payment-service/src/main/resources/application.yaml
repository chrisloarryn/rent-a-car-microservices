spring:
  application:
    name: payment-service
  cloud:
    config:
      profile: dev
  config:
    import: optional:configserver:http://localhost:8888
springdoc:
  group-configs:
    - group: ${spring.application.name}
      paths-to-match:
        - /api/**
