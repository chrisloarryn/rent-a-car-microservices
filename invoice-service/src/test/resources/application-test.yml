spring:
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: false
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://127.0.0.1:27017/invoice-service-test}
  kafka:
    listener:
      auto-startup: false
eureka:
  client:
    enabled: false
