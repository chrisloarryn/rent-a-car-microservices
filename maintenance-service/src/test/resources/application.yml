spring:
  application:
    name: maintenance-service
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      fail-fast: false
eureka:
  client:
    enabled: false
